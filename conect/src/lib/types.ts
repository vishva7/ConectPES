export interface details {
  name: string;
  role: string;
  image: string;
  desc: string;
}

export interface publications {
  title: string;
  authors: string;
  summary: string;
  link: string;
}

export interface CarouselData {
  title: String;
  description: String;
  image: String;
}
